#include <iostream>
using namespace std;
int main()
{
    int so1, so2, so3, so4;
    cin >> so1 >> so2;
    cout << "tong so nhat va so shu hai la " << so1 * so2 << endl;
    cin >> so3 >> so4;
    cout << "tich so shu ha va so shu tu la " << so3 * so4 << endl;
    return 0;
}
